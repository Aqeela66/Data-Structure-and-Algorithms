//You are given an array [1, 9, 20, 20, 4, 0, 3, 4] . Add 9 to the end of the array.

let array = [1, 9, 20, 20, 4, 0, 3, 4];

array.push(9);         //pop use for add a number in the end
console.log(array);
for (let i = 0; i >= array.length; i++) {
    console.log(array[i]);
}